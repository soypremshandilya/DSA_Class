class Main{
    public static void main(String[] args) {
        int n = arr.length;
        int[] ans = new int[A]
        int index = n-1;
        for(int i=n-1; i>=0; i--){
            while(!st.isEmpty() && arr[i] >=st.peek())
            {
                st.pop();
            
            }
            if(st.isEmpty()){
                ans[i]  == -1;
            }
            else{
                ans[i]  = -1;
            }
            else{
                ans[i] = st.peek();
            }
            st.push(arr[i]);
        }
        ArrayList<integer> st=newArrayList<>();
        for(int i =0; i<n ; i++){
            al.add(ans[i]);
        }
        return al;
    }


}